import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Study Best - AI-Powered IELTS Preparation",
  description:
    "Prepare for IELTS with Study Best, an AI-powered learning platform featuring personalized study plans, adaptive practice, AI writing and speaking evaluation, real exam simulations, progress tracking, and smart recommendations to maximize your IELTS score.",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
